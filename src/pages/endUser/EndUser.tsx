import React from 'react';
import PropTypes from 'prop-types';
import Constractor from '../constractor/Constractor';

EndUser.propTypes = {
    
};

function EndUser(props: any) {
    return (
        <div>
            <Constractor />
        </div>
    );
}

export default EndUser;